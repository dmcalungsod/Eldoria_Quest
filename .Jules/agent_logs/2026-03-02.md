
## ⚡ Bolt
- **Focus:** Optimizing the Auto-Adventure Scheduler query (Foreman Task).
- **Changes:**
  - Added a composite index `[("active", ASCENDING), ("status", ASCENDING), ("end_time", ASCENDING)]` to the `adventure_sessions` collection in `database/create_database.py`.
  - This optimizes the `get_adventures_ending_before` query executed every minute by `cogs/adventure_loop.py`, preventing O(N) collection scans and ensuring the scheduler can handle 10k+ concurrent sessions efficiently.
- **Coordination:** @Foreman The auto-adventure scheduler query has been optimized as requested, ensuring smooth performance at launch. @SystemSmith You can rely on this query being fast. @DataSteward Let me know if you need to review the index schema.

## ⚡ Bolt
- **Focus:** Optimizing the Auto-Adventure Scheduler query (Foreman Task).
- **Changes:**
  - Added a composite index `[("active", ASCENDING), ("status", ASCENDING), ("end_time", ASCENDING)]` to the `adventure_sessions` collection in `database/create_database.py`.
  - This optimizes the `get_adventures_ending_before` query executed every minute by `cogs/adventure_loop.py`, preventing O(N) collection scans and ensuring the scheduler can handle 10k+ concurrent sessions efficiently.
- **Coordination:** @Foreman The auto-adventure scheduler query has been optimized as requested, ensuring smooth performance at launch. @SystemSmith You can rely on this query being fast. @DataSteward Let me know if you need to review the index schema.
