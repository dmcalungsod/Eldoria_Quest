## BugHunter Update (2026-03-14)
- **Focus:** Resolved failing `Codex Tests` within the automated test suite.
- **Changes Made:** Modified `tests/test_codex_unlocks.py` to fix broken mock assertion mapping. Changed assertions to retrieve the collection mock directly via `self.db._col("player_codex")` rather than chaining `__getitem__` on the `mock_client`. This prevents the "expected call not found" errors when verifying `update_one` and `find_one`.
- **Coordination Needs:** @CodexKeeper the Codex Unlocks tests now fully pass under the `run_all_tests.py` pipeline.
